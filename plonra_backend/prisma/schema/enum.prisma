enum EventType {
    PUBLIC
    PRIVATE
}

enum EventStatus {
    UPCOMING
    ONGOING
    COMPLETED
    CANCELLED
}

enum RegistrationStatus {
    PROCESSING
    APPROVED
    REJECTED
    BANNED
}

enum PaymentStatus {
    PAID
    UNPAID
    FREE
}

enum InvitationStatus {
    PENDING
    ACCEPTED
    REJECTED
}

enum NotificationType {
    PAYMENT_SUCCESS
    PAYMENT_FAILED
    JOIN_REQUEST
    REQUEST_APPROVED
    REQUEST_REJECTED
    REQUEST_PENDING
    INVITATION_RECEIVED
    EVENT_UPDATED
    EVENT_CANCELLED
    BANNED_FROM_EVENT
    REVIEW_POSTED
}

enum Gender {
    MALE
    FEMALE
    OTHER
}

enum Role {
    USER
    HOST
    ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
    BANNED
}
